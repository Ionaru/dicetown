export enum RoomStatus {
  Waiting = "waiting",
  Playing = "playing",
  Finished = "finished",
}

export enum TurnPhase {
  Rolling = "rolling",
  Income = "income",
  Buying = "buying",
  Cleanup = "cleanup",
}
